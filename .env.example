# ── LLM (at least one) ────────────────────────────────────────────────────────
GROQ_API_KEY=            # free: https://console.groq.com/keys   (primary)
OPENROUTER_API_KEY=      # fallback, ":free" models only unless you change OPENROUTER_MODEL
LLM_PROVIDER=groq                       # groq | openrouter
LLM_FALLBACK_PROVIDERS=openrouter
GROQ_MODEL=llama-3.3-70b-versatile
OPENROUTER_MODEL=minimax/minimax-m2.7:free   # old paid: meta-llama/llama-3.3-70b-instruct

# ── Retrieval ─────────────────────────────────────────────────────────────────
GEMINI_API_KEY=          # free: https://aistudio.google.com/apikey (embeddings, must start with AIza)
PINECONE_API_KEY=
PINECONE_INDEX=scaler

# ── Calendar booking ──────────────────────────────────────────────────────────
CAL_API_KEY=
CAL_USERNAME=pranay-reddy-mqfpgr

# ── Demo guard rails ──────────────────────────────────────────────────────────
DAILY_REQUEST_CAP=200
CONTACT_EMAIL=pranayreddy672@gmail.com
